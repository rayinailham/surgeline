# SurgeLine — CLIENT_REPORT (bentuk laporan, diisi P13)

> STUB. Bentuk, nada, dan irama laporan ke klien. Diisi di P13.

Prinsip (diwarisi driftwatch): **0 jargon**, angka > klaim, tiap angka bisa ditelusuri ke DB.
Akan memuat: ringkasan run (total/berhasil/gagal/dead), throughput + ekstrapolasi 6 juta record,
bukti crash-recovery (1 paragraf + angka), dan pernyataan lingkup legal (target milik sendiri).
Deliverable: digest `.md` + `REPORT.xlsx` (sheet: Ringkasan, Per-status, Throughput, Kegagalan, Bukti).
