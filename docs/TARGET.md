# SurgeLine — TARGET (diisi di P1)

> STUB. Spesifikasi app form yang diuji. Diisi di P1, dilengkapi P2.

Akan mendefinisikan:
- Endpoint: `GET /` (form), `POST /submit` (kirim), halaman hasil bernomor konfirmasi, `GET /health`.
- Field form + aturan validasi (mana yang wajib, format, panjang).
- Bentuk halaman hasil: selector nomor konfirmasi yang ditangkap worker (D6).
- Cara menjalankan: `docker compose up -d`, port `:8110` (D9).
